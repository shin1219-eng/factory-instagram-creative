.DS_Store
node_modules/

# Output artifacts (tracked only via .gitkeep)
04_OUTPUT/**
!04_OUTPUT/**/
!04_OUTPUT/.gitkeep
!04_OUTPUT/**/.gitkeep

# Run logs (tracked only via .gitkeep)
05_LOGS/runs/**
!05_LOGS/runs/.gitkeep

# Capture artifacts (Spector / video / screenshots)
05_LOGS/captures/**

# Spector bundle (downloaded)
scripts/spector_capture/vendor/spector.bundle.js
